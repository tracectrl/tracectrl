Now we upload a different invoice. This one has instructions embedded directly in the PDF — hidden from a human reviewer, but visible to the AI. DocAI extracts the text and writes it into shared context — including the injected payload. The Orchestrator reads that context and follows the instructions. PolicyAgent? Skipped. VendorIntel? Skipped. RiskAgent? Bypassed. The pipeline routes directly to PaymentAgent. An unauthorised payment executes. The agent did exactly what it was told — by an attacker.
