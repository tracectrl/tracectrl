This is FinFlow running normally. A clean invoice comes in through the portal. The Orchestrator picks it up and routes it through the pipeline. DocAI parses the document. PolicyAgent validates it against financial controls. VendorIntel checks the vendor. RiskAgent runs the sanctions check. Payment executes. Every step is captured by TraceCtrl — every agent, every tool call, every decision. This is your baseline topology.
